<h1>New invitation</h1>

<%= semantic_form_for(@invitation) do |f| %>
    <%= f.input :recipient %>
    <%= f.input :first_move, :as => :enum %>
    <%= f.buttons %>
<% end %>

<%= link_to 'Back', invitations_path %>
